import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textInput: {
    borderColor: "#000000",
    borderWidth: 1,
    paddingHorizontal: 5,
    paddingVertical: 15,
    marginVertical: 5,
    borderRadius: 10,
    backgroundColor: "#fff",
  },

  inputError: {
    textAlign: "center",
    color: "red",
    fontSize: 12,
    marginBottom: 0,
  },

  inputContainer: {
    marginBottom: 15,
  },
});
